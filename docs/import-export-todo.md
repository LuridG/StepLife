# StepLife 生活记录 导入/导出 剩余计划（TODO）

> 版本：v1.1 计划（2026-08）；已实现部分见 [store-import-export-implemented.md](store-import-export-implemented.md)，历史方案见 [history/store-import-export-plan.md](history/store-import-export-plan.md)
> 状态：以下为 P5/P6 及后续扩展，按优先级排序；大改动先出方案、用户确认后再实施

## P5 模板差异化适配（导入字段联动）

- [ ] **dining 餐饮**：账单识别到菜名时联动 `store_menu_items`（名称/规格/价格自动入库，避免纯文本）；招牌推荐菜与菜单登记联动。
- [ ] **movie 影视**：导入观看记录联动集数（`extras` 保留），可匹配 TMDB 元数据；「在追/想看/搁置/抛弃」状态快捷设置。
- [ ] **basket 菜篮子**：导入「品类+品牌」价格记录（如 佳农香蕉/辉众香蕉），价格走势/总表联动；识别到已存在品类+品牌时合并价格打卡。
- [ ] **snack 零食**：品牌入 `extrasJson.brand`，tag 分类导入联动二级筛选。
- [ ] **shopping/book/place/generic**：通用 cost/time/memo 骨架即可，逐步精修。

## P6 收尾（交付质量）

- [ ] 模板示例 JSON 文件（与导出 Schema 完全一致），导入弹窗内置可复制/导出。
- [ ] README 与 docs 更新（含本批次实现的说明）。
- [ ] 补充 `flutter test` 用例：详情页锁定回归、秒级合并回归、敏感词脱敏回归、防撞名回归。
- [ ] 全量回归 `flutter analyze` + `flutter test`。

## 扩展 E1：图片导出（zip 带图）

- [ ] `StoreExporter` 预留 `includeImages` 钩子已就位；实现 zip 打包（图片复制进缓存目录后再打包，沿用「选图必须 copyToCache」规范）。
- [ ] 导出界面提供「纯 JSON / 带图 zip」选择（体积提示），导入端自动识别 zip 并还原图片到缓存目录。
- [ ] 图片路径在导出 JSON 中保持相对路径，导入时重建映射，禁止直接写相册原路径。

## 扩展 E2：导入层级与交互细化

- [ ] **L2 新建项目确认**：识别到新店铺/新片名时，预览先进入「新建项目确认」（可改名称/分类/模板），确认后建项目再追加打卡；账单里出现「今日新消费店铺」时自动识别店铺名。
- [ ] **模拟导入增强**：导入前「模拟试运行」可设置归属/字段修正，真实导入复用同一参数；模拟与真实结果必须一致。
- [ ] **导入结果报告**：落库后给出失败清单（哪条为何跳过），支持部分成功不整体回滚的策略选择。
- [ ] **模板文件校验提示**：非法 JSON 报错定位到具体条目（`logs[3].timestamp 格式非法` 等）已实现，补充对文件内重名分类/项目的校验。

## 扩展 E3：AI 结构化增强

- [ ] 账单多笔分割强化（一张图 10 笔等批量导入）已实现，继续加：金额负号/中文数字/多币种处理。
- [ ] 菜篮子/零食模板的 AI 字段（品牌、单位、数量、单价）结构化，识别是否已存在该品类+品牌。
- [ ] 敏感词脱敏支持正则自定义（当前为关键词+数字串），可加白名单/黑名单管理。

## 验收标准（统一）

- 每项完成后 `flutter analyze` 0 错误 + 相关 `flutter test` 通过。
- 用户电脑确认后再进入下一项；是否打包/发布由用户单独下令。
